{
    "name": "lemon-squiz-app",
    "version": "1.0.0",
    "main": "index.js",
    "repository": {},
    "license": "MIT",
    "dependencies": {
        "concurrently": "^3.1.0"
    },
    "scripts": {
    "install": "(cd quiz_app && yarn) && (cd quiz_app_server && yarn)",
    "start": "concurrently \"cd quiz_app && set PORT=3000 yarn start\" \"cd quiz_app_server && set PORT=3002 yarn start\"",
    "build": "concurrently \"cd quiz_app && yarn build\" \"cd quiz_app_server && yarn build\"",
    "start:prod": "cd quiz_app_server && yarn start:prod",
    "heroku-postbuild": "yarn build"
  },
  "engines": {
    "node": "8.5.0"
 }
}